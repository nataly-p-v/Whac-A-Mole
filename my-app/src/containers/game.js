import React from 'react'
import { connect } from 'react-redux'
import { GameAction } from '../actions'

const Game = ({ dispatch }) => {
  return (
    <div>
        game
    </div>
  )}

export default connect()(GameAction)